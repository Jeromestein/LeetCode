|Category|Difficulty|Likes|Dislikes|
|---|---|---|---|
|algorithms|Medium (51.00%)|1618|169|

**Tags**

[`array`](https://leetcode.com/tag/array?source=vscode "https://leetcode.com/tag/array?source=vscode") | [`two-pointers`](https://leetcode.com/tag/two-pointers?source=vscode "https://leetcode.com/tag/two-pointers?source=vscode") #two-pointers #sorting #binary-search 

**Companies**

`google`

Given an array of `n` integers `nums` and an integer `target`, find the number of index triplets `i`, `j`, `k` with `0 <= i < j < k < n` that satisfy the condition `nums[i] + nums[j] + nums[k] < target`.

**Example 1:**

```
Input: nums = [-2,0,1,3], target = 2
Output: 2
Explanation: Because there are two triplets which sums are less than 2:
[-2,0,1]
[-2,0,3]
```

**Example 2:**

```
Input: nums = [], target = 0
Output: 0
```

**Example 3:**

```
Input: nums = [0], target = 0
Output: 0
```

**Constraints:**

- `n == nums.length`
- `0 <= n <= 3500`
- `-100 <= nums[i] <= 100`
- `-100 <= target <= 100`

---

[Submissions](https://leetcode.com/problems/3sum-smaller/submissions/?source=vscode "https://leetcode.com/problems/3sum-smaller/submissions/?source=vscode") | [Solution](https://leetcode.com/problems/3sum-smaller/solutions/?source=vscode "https://leetcode.com/problems/3sum-smaller/solutions/?source=vscode")

```python

class Solution:
    def threeSumSmaller(self, nums, target):
        nums.sort()
        total = 0
        for i in range(len(nums) - 2):
            total += self.twoSumSmaller(nums[i+1:], target - nums[i])
        
        return total

    def twoSumSmaller(self, nums, target):
        count = 0
        left, right = 0, len(nums) - 1
        
        while left < right:
            if nums[left] + nums[right] < target:
                count += right - left
                left += 1
            else:
                right -= 1
        
        return count

```